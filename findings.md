# 结构性审阅发现

## 初始记录

- 2026-09-11：本轮审阅目标是从用户视角检查空台账、自定义资产和统一编辑能力。
- 现有产品文档已声明支持资产/权益，但必须进一步核对 demo 的真实入口和数据模型。
- 工作树已有用户相关未提交改动；审阅与修复必须在其上增量进行。
- `viewAction()` 的“添加资产”和模板按钮都走 `addtmpl`，当前处理器只显示“演示：已添加”，不会创建记录。
- 详情页按现金、权益、实物分支提供局部操作，没有统一的名称/分类/通用字段编辑入口。
- `isMemberAsset()` 用资产名称包含“会员”来推断类型；改名会改变分组和详情行为，违反名称与能力分离。
- 自然语言添加按分类创建基础记录，不能稳定创建带 `mileage` 结构的通用里程资产。
- 应用把整个资产数组作为单一 SQLite state 保存；本轮为 SQLite 增加了 `schema_meta.version`，未识别的版本会直接拒绝打开，避免静默猜测或覆盖数据。真正的数据结构变更仍需单独设计和验证。

## 修复结果

- 已修复：模板和“添加资产”改为真实通用编辑器，用户可从空台账创建资产。
- 已修复：详情页加入统一“编辑信息”，里程保留“更新里程”快捷操作。
- 已修复：自然语言添加复用通用资产构造器；里程字段可由结构化结果进入 `mileage` 模型。
- 已修复：资产搜索可按名称、分类和单位筛选；用户自定义名称不会改变会员类型判断。
- 已修复：通用编辑保存后的延时路由竞态，路由切换会取消旧定时器。

## 仍然开放

- CI 失败证据：Windows `npm run test:native` 曾在 `tools/native-app.mjs` 等待 CDP 时收到 `ECONNREFUSED 127.0.0.1:<port>`；GitHub 手动运行的 Mac 构建可通过，Node 20 只是弃用警告。
- Windows CI 修复已由 GitHub runner 验证：每次原生测试启动使用独立 `WEBVIEW2_USER_DATA_FOLDER`，并由 Tauri 原生层注入测试 CDP 参数，避免同一 profile 的旧 WebView2 浏览器进程或提升权限导致调试端口失效；run `34774524823` 的 Windows 原生和已安装包测试通过。
- 远程验证还暴露并修复了应用层延时路由竞态：用户主动切换页面时会取消旧的待执行跳转，避免旧定时器把当前页面覆盖；同一 run 的 Windows/macOS build 均最终成功。
- 前端已拆为页面骨架、样式、资产领域、应用编排和本地存储五个文件；视图与交互仍集中在 `demo/app.js`，但已经不再和页面骨架、样式、数据存取混在同一文件。
- SQLite 仍以单一数组快照保存；当前 schema 版本为 1，并有严格版本门禁，尚未引入隐式兼容迁移。
- 金融资产页面仍未开发；部分“转账记录”“核对会籍”等历史按钮按当前产品范围保留为非目标，不需要继续开放，也不应当作已实现能力。
